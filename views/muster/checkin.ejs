<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/product.css">
    <link rel="stylesheet" href="/css/forms.css">
    <style>
        button:disabled {
            background-color: grey;
            color: white;
            border: 1px solid grey;
        }

        button:disabled:hover {
            background-color: grey;
        }
    </style>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>

            <main>
                <%- include('../includes/nav-child.ejs') %>
                <div class="grid">
                    <article>
                        <div class="form-group">
                            <label for="annualLeave">Họ Tên</label>
                            <input type="number" name="annualLeave" id="annualLeave" disabled placeholder="<%= staffs.name %>" />
                        </div>
                        <form action="/muster/checkin" method="POST">
                            <div class="form-group">
                                <label for="workdress">Nơi làm việc</label>
                                <select name="workdress" id="workdress">
                                    <option value="Nhà">Nhà</option>
                                    <option selected value="Công Ty">Công Ty</option>
                                    <option value="Khách Hàng">Khách Hàng</option>
                                </select>
                                <br /><br />
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <button class="btn" type="submit">Điểm Danh</button>
                            </div>
                        </form>
                    </article>
                </div>
            </main>
            <%- include('../includes/end.ejs') %>